extends layout

block content
    main
    nav
        .nav-wrapper
        a.brand-logo(href='#!') Chopper Drive
    section
        article.row
        .container.center
            .col.s12.m12
            h4  Solicitar contrase&ntilde;a
            i.large.material-icons account_circle
            .col.s12
            p Introduce la clave de seguridad que se ha enviado a tu correo electronico
        article.row.container
        form.col.s12(action='', method='POST')
            .row
            .container.center
                .input-field.col.s12
                    i.material-icons.prefix lock
                    label.active(for='cla') Clave de seguridad
                    input#first_name.validate(type='text', name='cla')
                a.waves-effect.waves-light.btn.btn-color(href='/drive/cambiarContra') Verificar
